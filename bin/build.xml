<?xml version="1.0" encoding="UTF-8"?>
<project basedir="../../TX24_LIB" default="compile" name="TX24">
	
	<!-- Project Default Directory -->
	<property name="src.dir" location="${basedir}\src"/>
	<property name="test.dir" location="${basedir}\test"/>
	<property name="lib.dir" location="${basedir}\lib"/>
	<property name="dist.dir" location="${basedir}\dist"/>
	<property name="classes.dir" location="${basedir}\classes"/>
	<property name="conf.dir" location="${basedir}\conf"/>
	<property name="doc.dir" location="${basedir}\doc"/>
	<property name="lib.jar" location="${dist.dir}\tx24_lib_2607.jar"/>
	<property name="inet.jar" location="${dist.dir}\tx24_inet_2607.jar"/>
	<property name="task.jar" location="${dist.dir}\tx24_task_2607.jar"/>
	
	<!-- Project Default Classpath -->
	<path id="project.classpath">
		<fileset dir="${lib.dir}">
			<include name="*.jar"/>
		</fileset>
	</path>

	<echo message ="#### SET PROJECT CLASSPATH ####"/>

	<!-- Make Directory -->
	<target name="init">
		<tstamp>
			<format property="time" pattern="yyyy-MM-dd hh:mm:ss" />
			<format property="jardate" pattern="yyMM" />
		</tstamp>
		<mkdir dir="${basedir}\classes"/>
		<mkdir dir="${basedir}\dist"/>
		<delete dir="${basedir}\doc"/>
		<mkdir dir="${basedir}\doc"/>
		<!--
		<javadoc destdir="${doc.dir}" classpathref="project.classpath" sourcepath="${src.dir}" 
			locale="ko_KR" encoding="UTF-8" docencoding="UTF-8" charset="UTF-8" use="true" failonerror="off" 
			packagenames = "syslink.lib.*"/>
		-->
	</target>

	<echo message ="#### COMPILE COMPLETE ####"/>

	<target name="compile-source" depends="init">
		<javac destdir="${basedir}\classes" srcdir="${src.dir}" includes="kr/tx24/**"  
			optimize="true" debug="true" deprecation="true" encoding="utf-8" includeantruntime="false" release="17">
			<classpath refid="project.classpath"/>
		</javac>
		<javac destdir="${basedir}\classes" srcdir="${test.dir}" includes="kr/tx24/**"  
		            optimize="true" debug="true" deprecation="true" encoding="utf-8" includeantruntime="false" release="17">
		            <classpath refid="project.classpath"/>
		        </javac>
		<copy todir="${classes.dir}">
            <fileset dir="${conf.dir}" includes="tx24.properties"/>
        </copy>
	</target>

	<target name="check-lib-jar" depends="compile-source">
		<uptodate property="lib.jar.uptodate" targetfile="${lib.jar}">
			<srcresources>
				<union>
					<fileset dir="${classes.dir}" includes="kr/tx24/lib/**"/>
					<fileset dir="${classes.dir}" includes="tx24.properties"/>
				</union>
			</srcresources>
		</uptodate>
	</target>

	<target name="jar-lib" depends="check-lib-jar" unless="lib.jar.uptodate">
		<jar destfile="${lib.jar}" basedir="${classes.dir}" includes="kr/tx24/lib/**,tx24.properties" excludes="kr/tx24/test/**">
			<manifest>
				<attribute name="Built-By" value="TX24"/>
				<attribute name="Jar-Title" value="Tx24 Core Library"/>
				<attribute name="Jar-Version" value="${jardate}"/>
				<attribute name="Last-Updated-Date" value="${time}"/>
			</manifest>
		</jar>
	</target>

	<target name="check-inet-jar" depends="compile-source">
		<uptodate property="inet.jar.uptodate" targetfile="${inet.jar}">
			<srcfiles dir="${classes.dir}" includes="kr/tx24/inet/**"/>
		</uptodate>
	</target>

	<target name="jar-inet" depends="check-inet-jar" unless="inet.jar.uptodate">
		<jar destfile="${inet.jar}" basedir="${classes.dir}" includes="kr/tx24/inet/**" excludes="kr/tx24/test/**">
            <manifest>
                <attribute name="Built-By" value="TX24"/>
                <attribute name="Jar-Title" value="Tx24 INet Server Library"/>
                <attribute name="Jar-Version" value="${jardate}"/>
                <attribute name="Last-Updated-Date" value="${time}"/>
            </manifest>
        </jar>
	</target>

	<target name="check-task-jar" depends="compile-source">
		<uptodate property="task.jar.uptodate" targetfile="${task.jar}">
			<srcfiles dir="${classes.dir}" includes="kr/tx24/task/**"/>
		</uptodate>
	</target>

	<target name="jar-task" depends="check-task-jar" unless="task.jar.uptodate">
		<jar destfile="${task.jar}" basedir="${classes.dir}" includes="kr/tx24/task/**" excludes="kr/tx24/test/**">
            <manifest>
                <attribute name="Built-By" value="TX24"/>
                <attribute name="Jar-Title" value="Tx24 Task Library"/>
                <attribute name="Jar-Version" value="${jardate}"/>
                <attribute name="Last-Updated-Date" value="${time}"/>
            </manifest>
        </jar>
	</target>

	<target name="check-all-jars" depends="check-lib-jar,check-inet-jar,check-task-jar">
		<condition property="all.jars.uptodate">
			<and>
				<isset property="lib.jar.uptodate"/>
				<isset property="inet.jar.uptodate"/>
				<isset property="task.jar.uptodate"/>
			</and>
		</condition>
	</target>

	<target name="report-no-jar-changes" depends="check-all-jars" if="all.jars.uptodate">
		<echo message="변경된 것이 없습니다. JAR 생성을 생략합니다."/>
	</target>

	<target name="compile" depends="jar-lib,jar-inet,jar-task,report-no-jar-changes"/>
</project>
